<table class="stats left">
	<thead>
		<tr class="sections">
			<th colspan="3" style="text-align: left;">Roster</th>
			<th colspan="4">Batting</th>
			<th colspan="4">Pitching</th>
		</tr>
		<tr>
			<th>Player</th>
			<th>Positions</th>
			<th>Bat/Throws</th>
			<th>AVG</th>
			<th>HR</th>
			<th>RBI</th>
			<th>SB</th>
			<th>ERA</th>
			<th>SO</th>
			<th>W</th>
			<th>L</th>
		</tr>
	</thead>
	<tbody class="players">
			<% @players.each do |p| %>
				<tr class="player">
					<% bs = BatterGameStat.where(:player_id => p.id) %>
						<td><%= link_to(p.fullname, player_path(p.id)) %></td>
						<td><%#= bs.position.position %></td>
						<td><%= p.bats %>/<%= p.throws %></td>
						<td><%= number_with_precision(((bs.sum(:hits).to_d/bs.sum(:ab).to_d).to_d), :precision => 3).to_s.sub("0", "") %></td>
						<td><%= bs.sum(:hr) %></td>
						<td><%= bs.sum(:rbi) %></td>
						<td><%= bs.sum(:sb) %></td>
					<% ps = PitcherGameStat.where(:player_id => p.id) %>
						<td><%= number_with_precision((((ps.sum(:er).to_d / ps.sum(:ip).to_d) * 9).to_d), :precision => 2) %></td>
						<td><%= ps.sum(:so) %></td>
						<td><%= ps.sum(:win) %></td>
						<td><%= ps.sum(:loss) %></td>
				</tr>
			<% end %>
	</tbody>
</table>

<table class="stats left">
	<thead>
		<tr class="sections">
			<th>Coaches</th>
		</tr>
		<tr>
			<th>Coach</th>
			<th>Manager</th>
			<th>1B</th>
			<th>3B</th>
		</tr>
	</thead>
	<tbody class="coaches">
			<% @coaches.each do |c| %>
				<tr class="coach">
					<td><%= c.fullname %></td>
					<td><%= "X" if c.manager %></td>
					<td><%= "X" if c.first_base %></td>
					<td><%= "X" if c.third_base %></td>
				</tr>
			<% end %>
	</tbody>
</table>
