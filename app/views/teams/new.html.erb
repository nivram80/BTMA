<%= form_for @team do |f| %>
	<%= f.label "Team Nickname" %>
	<%= f.text_field :nickname %>

	<%= f.label "City" %>
	<%= f.text_field :city %>

	<%= f.label "Team Logo" %>
	<%= f.text_field :logo %>

	<%= f.submit %>
<% end %>
